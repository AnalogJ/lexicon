---
engine_cmd_test: 'tox -e basic,py27'
engine_disable_lint: true
mgr_keep_lock_file: true
scm_enable_branch_cleanup: true

test_step:
  pre:
  - apt-get update && apt-get install -y --no-install-recommends dnsutils