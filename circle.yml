dependencies:
  override:
    - pyenv install 2.6.9
    - pyenv install 2.7.12
    - pyenv install 3.3.6
    - pyenv install 3.4.4
    - pyenv install 3.5.1
    - pip install tox tox-pyenv
    - pyenv local 2.6.9 2.7.12 3.3.6 3.4.4 3.5.1

test:
  override:
    - tox --tox-pyenv-no-fallback
