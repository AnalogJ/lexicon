dependencies:
  override:
    #pyenv install 2.7.10 already exists.
    - pyenv install 3.3.6
    - pyenv install 3.4.5
    - pyenv install 3.5.2
    - pip install tox tox-pyenv
    - pyenv local 2.7.10 3.3.6 3.4.5 3.5.2

test:
  override:
    - tox --tox-pyenv-no-fallback
